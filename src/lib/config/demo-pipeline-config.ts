/** Precomputed pipeline output for the envisioning demo note (heuristic path, no LLM). */
export const envisioningDemoPipelineSnapshotPath =
  "src/test/fixtures/envisioning-demo-pipeline-snapshot.json";

/** Pipeline flow steps completed when loading the demo snapshot (ingest → envisioning). */
export const envisioningDemoCompletedPipelineStepCount = 6;
